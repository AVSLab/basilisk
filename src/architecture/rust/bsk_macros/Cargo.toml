[package]
name = "bsk-macros"
version = "0.1.0"
edition = "2021"
description = "Procedural macros for native Basilisk Rust modules."

[lib]
proc-macro = true

[dependencies]
proc-macro2 = "1"
quote = "1"
syn = { version = "2", features = ["full"] }
